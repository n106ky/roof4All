<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/css/main.css" />
    <title>roof4All</title>
  </head>
  <body>
    <!-- Navbar -->
    <header><%- include('partials/navbar', {page: '/member'}) %></header>
    <main>
        <div>THIS IS YOUR INFO</div>
        <% if(session.user.verified == true) { %>
            <p>The membership has been verified.</p>
            <p>Member details as follow:</p>
            <p>User name: <%= user.userName %> </p>
            
        <% } else { %>
            <p>The membership has not been verified.</p>
            <a href="/aboutYou"><button>Verify your membership now</button></a>
        <% } %>
    </main>
  
  </body>
  <%- include('partials/footer', {page: '/member'}) %>
</html>
